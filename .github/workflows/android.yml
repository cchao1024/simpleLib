name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Gradle
      run: ./gradlew build
    - name: bintrayUpload
      run: ./gradlew clean build bintrayUpload -PbintrayUser=cchao1024 -PbintrayKey=eb36a90cb05f8f88d52d4f6a4e0873ac862e741a -PdryRun=false
